# Гистограмма + KDE
# import seaborn as sns
# import matplotlib.pyplot as plt
#
# sns.histplot(x, kde=True)
# plt.show()


# Смотри:
#
# симметрия?
# один пик или несколько?
# длинные хвосты?


# import scipy.stats as stats
#
# stats.probplot(x, dist="norm", plot=plt)
# plt.show()
# ✔️ Точки ≈ прямая → похоже на нормальное
# ❌ S-образная форма → скошенность / тяжёлые хвосты


# Скошенность (Skewness)
# from scipy.stats import skew
#
# skew(x)


# Интерпретация:
#
# Skew	Значение
# ≈ 0	симметричное
# > 0	правый хвост (логнормальное часто)
# < 0	левый хвост
#
# 📌 Практическое правило:
#
# |skew| < 0.5 → почти нормально
#
# 0.5–1 → умеренная скошенность
#
# > 1 → сильная скошенность


# from scipy.stats import kurtosis
#
# kurtosis(x, fisher=True)
# ≈ 0 → нормальное
#
# > 0 → тяжёлые хвосты (outliers)
#
# < 0 → плоское распределение


# ВАЖНО: на больших выборках почти всегда отвергают нормальность
#
# Shapiro–Wilk (до ~5000)
# from scipy.stats import shapiro
#
# stat, p = shapiro(x)
#
#
# p > 0.05 → не отвергаем нормальность
#
# p ≤ 0.05 → распределение не нормальное


# 7️⃣ Универсальный пайплайн (запомни)
# def analyze_distribution(x):
#     return {
#         "skew": skew(x),
#         "kurtosis": kurtosis(x),
#         "shapiro_p": shapiro(x[:5000])[1],
#     }
#
#
# 📌 Но интерпретируешь глазами и головой, не цифрами.
